1.      la a0, 8(s0)        # load the address of the next node into a0

-->     lw a0, 8(s0)        # load the address of the next node into a0

2.      add t1, s0, x0      # load the address of the array of current node into t1

-->     lw t1, 0(s0)        # load the address of the array of current node into t1

3.      add t1, t1, t0      # offset the array address by the count

-->     slli t3, t0, 2      # offset the array address by the count
        add t1, t1, t3      # offset the array address by the count

4.      jalr s1             # call the function on that value.

-->     addi sp, sp, -4     # save t1
        sw  t1, 0(sp)
        jalr s1             # call the function on that value.
        lw  t1, 0(sp)
        addi sp, sp, 4

5.      lw a1, 0(s1)        # put the address of the function back into a1 to prepare for the recursion

-->     add s1, a1, x0      # put the address of the function back into a1 to prepare for the recursion
